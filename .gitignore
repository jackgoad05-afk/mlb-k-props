.venv/
.env
__pycache__/
*.pyc
.claude/
.streamlit/secrets.toml

# Research/backtest-only data (Sessions 1-4) -- large, reproducible from src/fetch.py
# and src/fetch_odds.py, not needed at runtime by the live daily pipeline (Session 5).
# Season-2026-onward files (current-season rolling cache, current lag-season tables)
# ARE tracked -- see the negation rules below.
data/raw/external_odds/
data/raw/schedule_202[0-5].csv
data/raw/statcast_*_202[0-5].csv
data/raw/team_hitting_202[0-5].csv
data/raw/teams_202[0-5].csv
data/raw/pitching_bref_202[0-5].csv
data/raw/pitcher_gamelogs_202[0-5].csv
data/processed/games.parquet
data/processed/model_features_ml.parquet
data/processed/model_features_ks.parquet
data/processed/real_odds.parquet

# output/model_ml.joblib and output/calibrator_ml.joblib ARE tracked -- daily_ml.py's
# pure-prediction pipeline (paper/tracking only, no betting) needs them at runtime on
# a fresh Actions checkout. Same for output/ml_predictions_ledger.csv and
# output/ml_market_comparison.csv (the leaderboard + private market-favorite log).
# Everything else under output/ml_* is a one-off backtest artifact (Session 2/2.1/
# Track 1) -- large, reproducible from model_ml.py, not needed at runtime.
output/ml_*
!output/ml_predictions_ledger.csv
!output/ml_market_comparison.csv
output/ks_holdout_predictions.parquet
output/ks_prop_line_brier.csv
